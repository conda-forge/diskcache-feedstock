{%set name = "diskcache" %}
{%set version = "2.2.0" %}
{%set hash_type = "sha256" %}
{%set hash_val = "ccae795af37ad343d1bb68f94fd3180d7029e9cc11833f04cd92fd4c4c867bad" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash_val }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - diskcache

about:
  home: http://www.grantjenks.com/docs/diskcache/
  license: Apache 2.0
  summary: 'Disk and file backed cache.'

extra:
  recipe-maintainers:
    - pmlandwehr
